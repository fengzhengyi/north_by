<nav class="bg-gray-800 mb-1">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-center h-16">
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden ">
        <button class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white"
                aria-expanded="false"
                data-controller="menu-button"
                data-action="click->menu-button#click"
        >
          <span class="sr-only">Open main menu</span>
          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
          <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start text-gray-100">
        <div class="flex-shrink-0 flex items-center">
          <div class="text-2xl text-white font-bold flex gap-2 ">
            <div class="rounded-full">
              <%= embedded_svg 'music', class: 'text-white h-8 w-8' %>
            </div>
            <%= link_to("NorthBy", root_path) %>
          </div>
        </div>
        <div class="hidden sm:block sm:ml-6 ">
          <div class="flex items-center space-x-4 ">
            <% if current_user %>
              <div class="text-gray-300 px-3 py-2 font-bold">
                Welcome，<%= current_user&.full_name %>！
              </div>
              <%= button_to "Log out",
                            destroy_user_session_path,
                            method: :delete,
                            class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md  font-medium" %>
            <% else %>
              <%= link_to "Sign up",
                          new_user_registration_path,
                          class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md font-medium" %>
              <%= link_to "Log in",
                          new_user_session_path,
                          class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>