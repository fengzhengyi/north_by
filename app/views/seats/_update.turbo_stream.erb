<%= turbo_stream.replace(
  dom_id(row),
  partial: "rows/row",
  locals: { row:, concert:, user: }
)%>
<%= turbo_stream.replace(
  "subtotal",
  partial: "venues/subtotal",
  locals: { concert:, user:  }
) %>
